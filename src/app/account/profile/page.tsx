export default function ProfilePage() {
  return (
    <div>
      <h2 className="text-lg font-semibold text-graphite">Profile</h2>
      <p className="mt-2 max-w-lg text-sm text-muted">
        Sign-in isn&rsquo;t connected yet, so there&rsquo;s no persistent profile to manage
        here. Your saved properties and searches on this page work today via your browser —
        see Saved properties and Saved searches.
      </p>
    </div>
  );
}
